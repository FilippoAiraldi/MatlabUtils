function a = cell_like(b)
    % CELL_LIKE Array of cells with same size.
    %
    %   a = CELL_LIKE(b) Creates a matrix/array of cells
	%		with the same shape as b.
	
	a = cell(size(b));
end
